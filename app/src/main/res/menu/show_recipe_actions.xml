<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android1="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Search will display always -->
    <item android1:id="@+id/action_search"
          android1:title="@string/action_search"
          android1:icon="@drawable/ic_search_white_24dp"
          app:showAsAction="ifRoom"/>

    <item android1:id="@+id/action_remove"
          android1:title="@string/action_remove"
          app:showAsAction="never"/>

    <item android1:id="@+id/action_settings"
          android1:title="@string/action_settings"
          app:showAsAction="never"/>

    <item android1:id="@+id/action_credits"
          android1:title="@string/action_credits"
          app:showAsAction="never"/>

</menu>